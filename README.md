# Uptime checker

Checks if the URLs specified in urls.json are reachable & logs the status to the console. Built with Node.js.

## Setup

1. Clone the repository
2. `yarn` or `npm install`
3. Remove the .example extension from urls.json
4. Put the URLs you want to check in urls.json
5. `yarn run check` or `npm run check`
